{
  "_comment": "This file is a reference for the secret structure stored in Vault. Ofc the real values are never committed to git (Daaa) they live only in the running Vault instance. Populate Vault using the commands in docs/vault-setup.md.",
  "vsphere": {
    "user": "administrator@vsphere.local",
    "password": "<stored in vault at secret/vsphere>"
  },
  "ansible": {
    "user": "packer",
    "private_key": "<stored in vault at secret/ansible>"
  },
  "mysql": {
    "root_password": "<stored in vault at secret/mysql>"
  }
}
